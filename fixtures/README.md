# fixtures/

Committed, sanitized snapshot and finding JSON used by golden tests.
Populated from Phase 2 onward (`minimal_snapshot.json` in T2.1, full testbed
snapshots in T2.17). Never commit anything derived from a real customer database.
