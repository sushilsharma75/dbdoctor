# docs/discovery/

One file per discovery conversation (T1.2), named
`YYYY-MM-DD_<name>.md`, using `docs/call_notes_template.md`.

Gate 1 (T1.3) is scored from these files: ≥15 conversations, ≥5 agencies,
≥3 concrete "analyze my DB" commitments, ≥1 urgent pain, engine mix recorded
for every call. Target date: **19-Jul-2026** (see docs/DECISION.md).
